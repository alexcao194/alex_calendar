import 'package:flutter/material.dart';

class CalendarWeekPage extends StatelessWidget {
  const CalendarWeekPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this calendar week page'),
    );
  }
}
